{
  "name": "multi-agent-research-root",
  "version": "1.0.0",
  "description": "Root workspace workspace proxies for Multi-Agent Research Assistant",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
